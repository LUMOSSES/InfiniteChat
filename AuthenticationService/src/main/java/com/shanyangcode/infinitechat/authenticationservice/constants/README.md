# constants 包说明

## 包职责
统一管理“稳定、可复用、跨层共享”的常量定义。

## 为什么创建这个包
需求变化时，魔法字符串和数字最容易导致隐蔽 bug。  
常量集中后可避免：
- 重复硬编码
- 多处改值不一致
- 业务语义不清晰

## 包内结构
- `config`：配置类常量与超时参数。
- `user`：用户域错误码与业务前缀常量。

## 开发思考过程
1. 先把业务中重复出现的字面量抽出来。
2. 再按领域拆分常量空间，避免一个“大杂烩常量类”。
3. 最终让 Controller/Service 代码只表达业务，不写硬编码。

