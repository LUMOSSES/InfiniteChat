$kind: http-request
url: "{{baseUrl}}/api/v1/moment/like/{{testMomentId}}?momentId={{testMomentId}}\
  &userId={{loginUserId}}"
method: DELETE
queryParams:
  momentId: "{{testMomentId}}"
  userId: "{{loginUserId}}"
order: 3000
