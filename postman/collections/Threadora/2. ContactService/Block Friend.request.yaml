$kind: http-request
url: "{{baseUrl}}/api/v1/contact/{{loginUserId}}/block/{{targetUserId}}?userUui\
  d={{loginUserId}}&receiveUserUuid={{targetUserId}}"
method: POST
queryParams:
  userUuid: "{{loginUserId}}"
  receiveUserUuid: "{{targetUserId}}"
order: 9000
