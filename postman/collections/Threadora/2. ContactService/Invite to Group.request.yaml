$kind: http-request
url: "{{baseUrl}}/api/v1/contact/group/invite"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "sessionId": "{{testSessionId}}",
      "memberUuids": ["{{targetUserId}}"],
      "userUuid": "{{loginUserId}}"
    }
order: 12000
