$kind: http-request
url: "{{baseUrl}}/api/v1/contact/{{loginUserId}}/friend/{{targetUserId}}"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "msg": "Hi, let's be friends!"
    }
order: 3000
